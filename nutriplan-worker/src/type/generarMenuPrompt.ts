import type { MenuPromptParams } from "../type/prompt.type";

export function generarMenuPrompt({
  menuCount,
  objective,
  alimentos,
  meals,
}: MenuPromptParams): string {
  return `
  ... prompt ...
  `;
}
